import { motion } from "framer-motion";
import {
  ArrowUpRight,
  Brain,
  Car,
  Cloud,
  Cpu,
  GraduationCap,
  Mail,
  Rocket,
  Sparkles,
  Trophy,
} from "lucide-react";
import { FaGithub, FaLinkedin } from "react-icons/fa";
import VenomNetworkBackground from "./components/VenomNetworkBackground";

const projects = [
  {
    title: "SoccerTwos Tactics Copilot",
    tag: "RL + RAG",
    text: "Match intelligence for safe, aggressive, and baseline SoccerTwos agents.",
    stack: ["React", "FastAPI", "Azure", "RAG"],
    link: "https://github.com/shahriar-shahram/soccertwos-tactics-copilot",
    icon: Brain,
  },
  {
    title: "DemandPilot",
    tag: "Forecasting",
    text: "Retail demand forecasting pipeline with cleaning, modeling, and dashboarding.",
    stack: ["Python", "Pandas", "ML", "Streamlit"],
    link: "https://github.com/shahriar-shahram/DemandPilot",
    icon: Cloud,
  },
  {
    title: "Fresh Retail AI Copilot",
    tag: "Retail AI",
    text: "Large-scale analytics and AI copilot direction for fresh retail operations.",
    stack: ["Parquet", "Cloud", "Analytics", "AI"],
    link: "https://github.com/shahriar-shahram/fresh-retail-copilot",
    icon: Rocket,
  },
  {
    title: "ACC RL Simulator",
    tag: "Deep RL",
    text: "SAC, TD3, and DDPG for adaptive cruise control under packet loss.",
    stack: ["PyTorch", "SB3", "Gym", "RL"],
    link: "https://github.com/shahriar-shahram/acc-rl-simulator",
    icon: Cpu,
  },
  {
    title: "Energy-Aware EV Autonomy",
    tag: "MPC",
    text: "Energy-aware EV control using MPC, convex approximation, and optimization.",
    stack: ["MPC", "CVXPY", "EV", "Control"],
    link: "https://github.com/shahriar-shahram/energy-aware-ev-autonomy",
    icon: Car,
  },
];

const skills = [
  "Python",
  "PyTorch",
  "Stable-Baselines3",
  "Pandas",
  "FastAPI",
  "React",
  "Streamlit",
  "Docker",
  "Azure OpenAI",
  "Google Cloud",
  "CVXPY",
  "MPC",
  "CARLA",
  "SUMO",
];

function Chip({ children }: { children: string }) {
  return (
    <span className="rounded-full border border-white/10 bg-white/8 px-3 py-1 text-xs text-slate-300">
      {children}
    </span>
  );
}

export default function App() {
  return (
    <main className="relative min-h-screen overflow-hidden bg-transparent text-slate-100">
      <VenomNetworkBackground />

      <div className="relative z-10">
        <nav className="sticky top-0 z-50 border-b border-white/8 bg-slate-950/35 backdrop-blur-2xl">
          <div className="mx-auto flex max-w-7xl items-center justify-between px-6 py-4">
            <a
              href="#"
              className="liquid-border flex items-center gap-2 rounded-full bg-white/[0.04] px-4 py-2 font-semibold text-white"
            >
              <Sparkles className="h-5 w-5 text-sky-300" />
              Shahriar Shahram
            </a>

            <div className="hidden gap-6 text-sm text-slate-300 md:flex">
              <a href="#work" className="transition hover:text-white">Work</a>
              <a href="#research" className="transition hover:text-white">Research</a>
              <a href="#skills" className="transition hover:text-white">Skills</a>
              <a href="#contact" className="transition hover:text-white">Contact</a>
            </div>
          </div>
        </nav>

        <section className="mx-auto grid max-w-7xl gap-10 px-6 py-20 md:grid-cols-[1.08fr_.92fr] md:py-28">
          <motion.div
            initial={{ opacity: 0, y: 24 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.7 }}
          >
            <div className="liquid-border mb-6 inline-flex items-center gap-2 rounded-full bg-sky-300/10 px-4 py-2 text-sm text-sky-100 soft-shadow">
              <Trophy className="h-4 w-4" />
              Ph.D. Researcher · UCF ECE
            </div>

            <h1 className="max-w-4xl text-5xl font-semibold tracking-tight text-white md:text-7xl">
              AI systems for{" "}
              <span className="bg-gradient-to-r from-sky-300 via-cyan-200 to-indigo-300 bg-clip-text text-transparent">
                autonomy & forecasting.
              </span>
            </h1>

            <p className="mt-6 max-w-2xl text-lg leading-8 text-slate-300">
              I build ML, deep RL, MPC, and cloud AI systems for decision-making under uncertainty.
            </p>

            <div className="mt-8 flex flex-wrap gap-3">
              <a
                href="https://github.com/shahriar-shahram"
                className="accent-button inline-flex items-center gap-2 rounded-full px-5 py-3 text-sm font-semibold transition"
              >
                <FaGithub className="h-4 w-4" />
                GitHub
              </a>

              <a
                href="https://www.linkedin.com/in/shahriar-shahram/"
                className="secondary-button inline-flex items-center gap-2 rounded-full px-5 py-3 text-sm font-semibold transition"
              >
                <FaLinkedin className="h-4 w-4" />
                LinkedIn
              </a>

              <a
                href="https://scholar.google.com/citations?user=x1HlzQwAAAAJ"
                className="secondary-button inline-flex items-center gap-2 rounded-full px-5 py-3 text-sm font-semibold transition"
              >
                <GraduationCap className="h-4 w-4" />
                Scholar
              </a>

              <a
                href="/resume.pdf"
                className="resume-button inline-flex items-center gap-2 rounded-full px-5 py-3 text-sm font-semibold transition"
              >
                Resume
                <ArrowUpRight className="h-4 w-4" />
              </a>
            </div>

            <div className="mt-10 grid max-w-2xl grid-cols-2 gap-3 md:grid-cols-4">
              {[
                ["Ph.D.", "UCF ECE"],
                ["MPC", "EV Control"],
                ["Deep RL", "ACC/CACC"],
                ["Cloud AI", "RAG Apps"],
              ].map(([top, bottom]) => (
                <div
                  key={top}
                  className="liquid-border metric-tile rounded-2xl p-4 transition"
                >
                  <div className="text-lg font-semibold text-white">{top}</div>
                  <div className="mt-1 text-xs text-slate-400">{bottom}</div>
                </div>
              ))}
            </div>
          </motion.div>

          <motion.div
            initial={{ opacity: 0, scale: 0.96 }}
            animate={{ opacity: 1, scale: 1 }}
            transition={{ duration: 0.7, delay: 0.1 }}
            className="self-start"
          >
            <div className="grid gap-4">
              {[
                ["🤖", "Machine Learning", "Forecasting, data pipelines, AI copilots"],
                ["🚗", "Autonomous Systems", "ACC, CACC, EV energy optimization"],
                ["🧠", "Deep RL + MPC", "Control and learning under uncertainty"],
                ["☁️", "Cloud AI", "RAG, deployment, frontend/backend apps"],
              ].map(([emoji, title, text]) => (
                <div
                  key={title}
                  className="card-shine liquid-border glass-panel rounded-[1.6rem] p-5 soft-shadow transition hover:-translate-y-1 hover:bg-white/[0.08] hover:glow-shadow"
                >
                  <div className="flex items-start gap-4">
                    <div className="text-3xl">{emoji}</div>
                    <div>
                      <h3 className="text-xl font-semibold text-white">{title}</h3>
                      <p className="mt-1 text-sm text-slate-400">{text}</p>
                    </div>
                  </div>
                </div>
              ))}
            </div>
          </motion.div>
        </section>

        <section id="work" className="mx-auto max-w-7xl px-6 py-16">
          <div className="mb-10 flex flex-col justify-between gap-4 md:flex-row md:items-end">
            <div>
              <p className="section-kicker">Selected Work</p>
              <h2 className="mt-3 text-4xl font-semibold text-white md:text-5xl">
                Projects
              </h2>
            </div>
            <p className="max-w-xl text-slate-400">
              Research-to-product work across RL, forecasting, autonomy, and cloud AI.
            </p>
          </div>

          <div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">
            {projects.map((p) => {
              const Icon = p.icon;
              return (
                <motion.a
                  key={p.title}
                  href={p.link}
                  target="_blank"
                  rel="noreferrer"
                  whileHover={{ y: -6 }}
                  className="card-shine liquid-border glass-panel group rounded-[1.8rem] p-6 soft-shadow transition hover:bg-white/[0.08] hover:glow-shadow"
                >
                  <div className="mb-5 flex items-start justify-between">
                    <div className="rounded-2xl bg-sky-400/10 p-3 text-sky-300">
                      <Icon className="h-6 w-6" />
                    </div>
                    <ArrowUpRight className="h-5 w-5 text-slate-500 transition group-hover:text-sky-300" />
                  </div>

                  <p className="text-sm font-medium text-sky-300">{p.tag}</p>
                  <h3 className="mt-2 text-xl font-semibold text-white">{p.title}</h3>
                  <p className="mt-3 min-h-[70px] text-sm leading-6 text-slate-400">
                    {p.text}
                  </p>

                  <div className="mt-5 flex flex-wrap gap-2">
                    {p.stack.map((item) => (
                      <Chip key={item}>{item}</Chip>
                    ))}
                  </div>
                </motion.a>
              );
            })}
          </div>
        </section>

        <section id="research" className="mx-auto max-w-7xl px-6 py-16">
          <div className="grid gap-5 md:grid-cols-3">
            <div className="liquid-border glass-panel-strong rounded-[2rem] p-7 soft-shadow md:col-span-2">
              <p className="section-kicker">Research</p>
              <h2 className="mt-3 text-4xl font-semibold text-white">
                Energy-aware autonomy
              </h2>
              <p className="mt-4 max-w-3xl leading-7 text-slate-300">
                My Ph.D. work focuses on autonomous and connected EVs under packet loss, noisy preview, and real-time constraints.
              </p>
            </div>

            <div className="card-shine liquid-border glass-panel rounded-[2rem] p-7 soft-shadow">
              <p className="text-sm text-slate-400">Selected publication</p>
              <h3 className="mt-3 text-xl font-semibold text-white">IEEE OJ-ITS 2025</h3>
              <p className="mt-3 text-sm leading-6 text-slate-400">
                Information-driven MPC with adaptive partitioning for EV energy optimization.
              </p>
            </div>
          </div>
        </section>

        <section id="skills" className="mx-auto max-w-6xl px-6 py-16 text-center">
          <p className="section-kicker">Stack</p>
          <h2 className="mt-3 text-4xl font-semibold text-white">Tools I Use</h2>

          <div className="mt-8 flex flex-wrap justify-center gap-3">
            {skills.map((s) => (
              <span
                key={s}
                className="rounded-full border border-white/10 bg-white/[0.06] px-4 py-2 text-sm text-slate-300 backdrop-blur"
              >
                {s}
              </span>
            ))}
          </div>
        </section>

        <section id="contact" className="mx-auto max-w-5xl px-6 py-20">
          <div className="liquid-border glass-panel-strong rounded-[2rem] p-8 text-center glow-shadow md:p-12">
            <p className="section-kicker">Contact</p>
            <h2 className="mt-4 text-4xl font-semibold text-white md:text-5xl">
              Let’s connect.
            </h2>
            <p className="mx-auto mt-4 max-w-xl text-slate-300">
              ML, autonomy, data science, forecasting, and cloud AI product roles.
            </p>

            <div className="mt-8 flex flex-wrap justify-center gap-3">
              <a
                href="mailto:shahriar.shahram95@gmail.com"
                className="accent-button inline-flex items-center gap-2 rounded-full px-5 py-3 text-sm font-semibold transition"
              >
                <Mail className="h-4 w-4" />
                Email
              </a>

              <a
                href="https://github.com/shahriar-shahram"
                className="secondary-button inline-flex items-center gap-2 rounded-full px-5 py-3 text-sm font-semibold transition"
              >
                <FaGithub className="h-4 w-4" />
                GitHub
              </a>

              <a
                href="https://www.linkedin.com/in/shahriar-shahram/"
                className="secondary-button inline-flex items-center gap-2 rounded-full px-5 py-3 text-sm font-semibold transition"
              >
                <FaLinkedin className="h-4 w-4" />
                LinkedIn
              </a>
            </div>
          </div>
        </section>

        <footer className="border-t border-white/8 px-6 py-8 text-center text-sm text-slate-500">
          © {new Date().getFullYear()} Shahriar Shahram
        </footer>
      </div>
    </main>
  );
}
